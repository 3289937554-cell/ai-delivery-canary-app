# User Flows

schema_version: user-flows/v1
issue_id: GH-123

## FLOW-001 Primary Outcome

Describe the user's entry point, intent, permissions, decisions, system responses, recovery paths, and successful exit. Keep this flow synchronized with `functional-map.json` and every screen state in `screen-state-matrix.json`.
